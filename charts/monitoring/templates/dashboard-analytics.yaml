apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-analytics
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/instance: monitoring
data:
  plausible-analytics.json: |
{{ .Files.Get "dashboards/plausible-analytics.json" | indent 4 }}

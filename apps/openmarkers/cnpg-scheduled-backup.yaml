apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: openmarkers-postgres-daily-backup
  namespace: openmarkers
spec:
  schedule: "0 30 3 * * *"
  backupOwnerReference: self
  cluster:
    name: openmarkers-postgres

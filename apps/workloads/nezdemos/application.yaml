apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nezdemos
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: homelab
  source:
    repoURL: https://github.com/nezdemkovski/homelab-gitops.git
    targetRevision: master
    path: charts/nezdemos
    helm:
      releaseName: nezdemos
  destination:
    server: https://kubernetes.default.svc
    namespace: nezdemos
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true


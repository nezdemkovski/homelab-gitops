apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: deadair-postgres-cnpg-daily-backup
  namespace: deadair
spec:
  schedule: "0 10 3 * * *"
  backupOwnerReference: self
  cluster:
    name: deadair-postgres-cnpg

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fizzy-data
  namespace: fizzy
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi

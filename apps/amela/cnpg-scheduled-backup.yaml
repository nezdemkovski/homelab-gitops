apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: amela-postgres-daily-backup
  namespace: amela
spec:
  schedule: "0 20 3 * * *"
  backupOwnerReference: self
  cluster:
    name: amela-postgres
